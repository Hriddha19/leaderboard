{
  "functions": {
    "api/[...params].js": {
      "maxDuration": 10
    }
  },
  "rewrites": [
    {
      "source": "/:type/:data",
      "destination": "/api/:type/:data"
    }
  ],
  "buildCommand": "mkdir -p public && cp index.html public/",
  "outputDirectory": "public"
}
