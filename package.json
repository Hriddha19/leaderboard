{
  "name": "discord-leaderboard-generator",
  "version": "1.0.0",
  "description": "Dynamic leaderboard image generator for Discord bots",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "mkdir -p public && cp index.html public/",
    "start": "vercel dev"
  },
  "dependencies": {
    "puppeteer-core": "^21.0.0",
    "@sparticuz/chromium": "^116.0.0"
  },
  "engines": {
    "node": "22.x"
  }
}
